# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Make' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Make
  pod 'Material', '~> 2.0'

  target 'MakeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MakeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
